<Window x:Class="Terminon.Views.PortForwardingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:Terminon.Converters"
        Title="Port Forwarding"
        Width="560" Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Port Forwarding Rules" FontSize="18" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}" Margin="0,0,0,16"/>

        <!-- Add new rule form -->
        <Border Grid.Row="1" Background="{DynamicResource Surface0Brush}" CornerRadius="6" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Label Content="Direction" Style="{DynamicResource FieldLabel}"/>
                        <ComboBox Style="{DynamicResource DarkComboBox}" ItemsSource="{Binding Directions}"
                                  SelectedItem="{Binding Direction}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <Label Content="Description (optional)" Style="{DynamicResource FieldLabel}"/>
                        <TextBox Style="{DynamicResource DarkTextBox}" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Label Content="Local Host" Style="{DynamicResource FieldLabel}"/>
                        <TextBox Style="{DynamicResource DarkTextBox}" Text="{Binding LocalHost, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <Label Content="Local Port" Style="{DynamicResource FieldLabel}"/>
                        <TextBox Style="{DynamicResource DarkTextBox}" Text="{Binding LocalPort, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <TextBlock Grid.Column="3" Text="→" VerticalAlignment="Bottom" HorizontalAlignment="Center"
                               Foreground="{DynamicResource OverlayBrush}" FontSize="16" Margin="0,0,0,8"/>
                    <StackPanel Grid.Column="4">
                        <Label Content="Remote Host" Style="{DynamicResource FieldLabel}"/>
                        <TextBox Style="{DynamicResource DarkTextBox}" Text="{Binding RemoteHost, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="6">
                        <Label Content="Remote Port" Style="{DynamicResource FieldLabel}"/>
                        <TextBox Style="{DynamicResource DarkTextBox}" Text="{Binding RemotePort, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <Button Grid.Row="4" Content="Add Rule" Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Right" Command="{Binding AddCommand}"/>
            </Grid>
        </Border>

        <!-- Rules list -->
        <ListView Grid.Row="2" ItemsSource="{Binding Rules}"
                  SelectedItem="{Binding Selected}"
                  Style="{DynamicResource DarkListView}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Rule" Width="380" DisplayMemberBinding="{Binding DisplayText}"/>
                    <GridViewColumn Header="Active" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Remove Selected" Style="{StaticResource DangerButton}"
                    Command="{Binding RemoveCommand}"/>
            <Button Grid.Column="2" Content="Cancel" Style="{StaticResource SecondaryButton}"
                    Command="{Binding CancelCommand}" IsCancel="True"/>
            <Button Grid.Column="4" Content="Save" Style="{StaticResource PrimaryButton}"
                    Command="{Binding OkCommand}" IsDefault="True"/>
        </Grid>
    </Grid>
</Window>
